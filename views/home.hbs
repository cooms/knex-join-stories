<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <link rel="stylesheet" href="skeleton.css">
    <link rel="stylesheet" href="normalize.css">
    <link rel="stylesheet" href="style.css">
    <title>Wombles</title>
  </head>
  <body>
    <h1>Wombles!</h1>
    {{#each data}}
    <table>

    <tr>
      <td>{{name}}</td>
      <td><a href="/delete?wom_id={{id}}"><button>delete</button></a></td>
  </tr>
    <br>
    </table>
    {{/each}}

    <br>
    <a href="/outfit"><button>Outfits</button></a>
    <a href="/trash"><button>Rubbish</button></a>
    <br>
    Add a womble:
    <form action="/newWomble" method="post">
      <input name="name" placeholder="womble name">
      <select name="characteristic">
        {{#each data}}
        <option value="{{characteristic_id}}">{{outfit}}</option>
        {{/each}}
      </select>
      <select name="rubbish">
        {{#each data}}
        <option value="{{rubbish_id}}">{{trash}}</option>
        {{/each}}
      </select>
      <input type="submit">
    </form>

    Mod a womble:
    <form action="/update" method="post">

      <select name="id">
        {{#each data}}
        <option value="{{id}}">{{name}}</option>
        {{/each}}
      </select>

      <select name="characteristic">
        {{#each data}}
        <option value="{{characteristic_id}}">{{outfit}}</option>
        {{/each}}
      </select>

      <input type="submit">
    </form>

  </body>
</html>
